
'use client';

import { SharedAppLayout } from '@/components/layout/shared-app-layout';

export default function NotificationsLayout({ children }: { children: React.ReactNode }) {
  return <SharedAppLayout>{children}</SharedAppLayout>;
}
